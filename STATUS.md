# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package, offline unit tests, `MODEL_CARD.md`, the three standalone `TASK-INFERENCE` tutorials (`tutorials/moment_embeddings_colab.ipynb`, `tutorials/moment_imputation_colab.ipynb`, `tutorials/moment_anomaly_detection_colab.ipynb`) and the standalone `E2E` classification-adaptation tutorial (`tutorials/moment_classification_colab.ipynb`; the caller-trained head of `adaptation.py` on the pinned UCI HAPT sample — majority 16.7 % → 5-NN 72.2 % → linear probe 72.2 % → selected `unfrozen last 2 blocks + linear head` 77.8 % on 36 held-out windows in the local CPU execution of the committed blob recorded in `docs/release-verification.md`; generated by `tools/build_notebook.py` from four templates, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit is recorded in `docs/release-verification.md` for each notebook. The earlier repository-installing notebooks' live-notebook CI runs predate the standalone carrier and do not count for it.
