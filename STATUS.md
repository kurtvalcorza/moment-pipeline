# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package, offline unit tests, `MODEL_CARD.md`, and the three standalone `TASK-INFERENCE` tutorials (`tutorials/moment_embeddings_colab.ipynb`, `tutorials/moment_imputation_colab.ipynb`, `tutorials/moment_anomaly_detection_colab.ipynb`; generated by `tools/build_notebook.py` from three templates, DIMER Notebook Specification 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit is recorded in `docs/release-verification.md` for each notebook. The earlier repository-installing notebooks' live-notebook CI runs predate the standalone carrier and do not count for it.
